== Wikipages

Ruby on Rails-based contact/address book. Stores contacts with email address(es), street address(es), and phone number(s). As many unique (to the contact) phone numbers, street addresses, and email addresses can be created as the user's heart desires (and hardware allows).

Things you may want to cover:

* Our first Ruby on Rails project.

